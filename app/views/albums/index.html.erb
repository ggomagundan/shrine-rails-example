<table class="table table-hover">
  <tr>
      <th>Name</th>
  </tr>
  <% for photo in @photos %>
    <tr>
        <td><%= photo.title%></td>
        <td><%= image_tag  photo.image_url%></td>
    </tr>
  <% end %>
</table>
<%= link_to "New", new_album_path %>

